import { View, Text } from 'react-native';

export default function EventsPage() {
    return (
        <View>
            <Text style={{textAlign: "center", justifyContent: "center", alignItems: "center"}}>Events</Text>
        </View>
    );
}